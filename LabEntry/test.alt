var #last = (
    rec f => (#list & List => (
        match (list) {
            #ListSeq{head: #last, tail: #ListEnd{},} => last,
            #ListSeq{head: #b01, tail:#tail,} => f tail,
            #ListEnd{} => ListEnd{}
        }
    ))
) {
    last [1,4,3,2,0]
}